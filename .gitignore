# Local tooling / editor
.claude/
.vscode/
.idea/
.DS_Store

# Secrets — never commit
*.key
*.csr
*.pem
.env
.env.*
# …but the template carries no secrets and KURULUM.md tells you to copy it.
!.env.example

# Build artifacts
*.tar.gz
node_modules/
vendor/
.pytest_cache/
